# Environment Variables for EA Global Attendee Matching System
# Copy this file to .env and fill in your actual values

# ============================================================================
# Google Gemini API Key (Required for Python pipeline & Edge Functions)
# ============================================================================
# Get your key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# ============================================================================
# Supabase Configuration (Required for data upload & Edge Functions)
# ============================================================================
# Get these from your Supabase project dashboard: Settings → API

# Supabase Project URL
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Service Role Key (for admin operations - upload script)
# WARNING: This key has full access - keep it secret, never commit to git!
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Supabase Anon Key (for frontend - safe to expose publicly)
SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# Vue Frontend Configuration (Required for Vite development)
# ============================================================================
# These MUST have VITE_ prefix to be accessible in the frontend
# Same values as above, but prefixed for Vite

# Supabase URL for frontend (same as SUPABASE_URL)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon Key for frontend (same as SUPABASE_ANON_KEY)
# This is safe to expose in client-side code
VITE_SUPABASE_ANON_KEY=your-anon-key-here
